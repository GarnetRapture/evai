# EverTalk 프로젝트 에이전트 규칙

1. 언어, 저장소, 모델, 사용자 프로필처럼 런타임에서 바뀌는 설정을 수정할 때는 영속 설정만 바꾸고 끝내지 않는다. 해당 설정에서 파생된 프론트엔드 표시, 캐시, 활성 도메인 객체, 백엔드 세션과 상태 문구를 같은 작업에서 다시 계산하고 검증한다.
2. 다국어 UI를 추가하거나 변경할 때는 한국어·영어·간체 중국어 라벨을 함께 추가하고, 초기 렌더뿐 아니라 실행 중 언어 전환 뒤 기존 화면 상태와 비동기 상태 상세도 즉시 새 언어로 바뀌는지 확인한다.
3. 저장·복원·삭제 기능을 변경할 때는 IndexedDB 권위 저장소, 선택형 네이티브 SQLite 미러, JSON 백업, 자동 백업 폴더의 포함·제외·교체·실패 동작을 함께 정의하고 UI에 실제 동작을 명시한다.
4. 네이티브 확장 상태를 표시할 때는 하드코딩 경로·가상 연결 상태를 만들지 않는다. 실제 health 응답의 EXE 경로, SQLite 경로, PID 및 확인 가능한 파일 정보를 사용한다.
5. 한 도메인 상태가 여러 화면에 나타나면 단일 상태 원천을 공유한다. 좌측 목록, 최상단 바, 설정, 모바일 화면이 서로 다른 카운트나 상태를 독립적으로 만들지 않는다.
6. 브라우저 API가 제공하지 않는 OS 내부 경로나 하드웨어 정보를 추측하지 않는다. 제공 불가능한 값은 이유와 함께 명시하고, 확인 가능한 origin·논리 DB 이름·사용량·할당량을 표시한다.
7. 동적 변경 작업의 완료 조건에는 TypeScript 프로덕션 빌드, 린트, 관련 도메인 런타임 테스트와 설정 변경 전후 상태 검증을 포함한다.
